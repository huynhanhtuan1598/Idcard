const COLORS = {
    admin: {
        base: '#233044',
        background: '#f7f9fc',
    },
    blue: '#165FE6',
};

const PER_PAGE = 10;
const PAGE = 0;
const DATE_FORMAT = 'DD/MM/YYYY';

export { COLORS, PER_PAGE, PAGE, DATE_FORMAT };
